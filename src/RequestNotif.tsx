import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const RequestNotif = ()=>{
    return(
        <SafeAreaView>
            <Text>Requests Notifications page :3</Text>
        </SafeAreaView>
    )
}
export default RequestNotif