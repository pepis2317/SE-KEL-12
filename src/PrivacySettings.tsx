import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const PrivacySettings = ()=>{
    return(
        <SafeAreaView>
            <Text>Privacy Settings page :3</Text>
        </SafeAreaView>
    )
}
export default PrivacySettings