import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const SetupStudyLocation = ()=>{
    return(
        <SafeAreaView>
            <Text>Setup Study Location page :3</Text>
        </SafeAreaView>
    )
}
export default SetupStudyLocation